# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.playwright-mcp/
output/
*.local
.claude/

# Env files. Real secrets belong in *.local (ignored above). The tracked .env
# currently holds only public VITE_ vars, but ignore .env defensively so any
# future secret-bearing env file isn't auto-added. Keep the committed template.
.env
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
deno.lock
vite.config.ts.timestamp-*.mjs
